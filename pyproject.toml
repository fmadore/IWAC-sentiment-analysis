[tool.pytest.ini_options]
pythonpath = ["data-preprocess"]
testpaths = ["data-preprocess"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"] # Prompt text and URLs are intentionally kept intact.

[tool.ruff.format]
quote-style = "double"
